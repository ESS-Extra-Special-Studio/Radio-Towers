package org.berezka.berezkas_zombie_waves_api;

import net.minecraft.core.BlockPos;
import net.minecraftforge.event.TickEvent;

/**
 * Compile-time stub only — not shipped in the radiotowers JAR. Real class comes from Berezka's mod at runtime.
 * Lets {@link net.mcreator.radiotowers.mixin.WavesManagerMixin} compile when optional API jars are absent from Gradle.
 */
@SuppressWarnings("unused")
public class WavesManager {

    public void start(BlockPos pos) {
    }

    public void endWave() {
    }

    public void onServerTick(TickEvent.ServerTickEvent event) {
    }
}
